import { ActivityColorPipePipe } from './activity-color-pipe.pipe';

describe('ActivityColorPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ActivityColorPipePipe();
    expect(pipe).toBeTruthy();
  });
});
